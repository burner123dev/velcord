{
    "name": "libvesktop",
    "main": "build/Release/vesktop.node",
    "types": "index.d.ts",
    "devDependencies": {
        "node-addon-api": "^8.5.0",
        "node-gyp": "^11.4.2"
    },
    "scripts": {
        "build": "node -e \"if (process.platform !== 'win32') require('child_process').execSync('node-gyp configure build', {stdio: 'inherit'})\"",
        "clean": "node-gyp clean",
        "test": "npm run build && node test.js"
    }
}
